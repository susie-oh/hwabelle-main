import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://ryeviupygrrmoyxoycwl.supabase.co', process.env.SUPABASE_SERVICE_ROLE_KEY || ''); async function test() { const { data, error } = await supabase.from('entitlements').insert({ order_id: null, user_id: null, product_type: 'ai-designer', source: 'direct', status: 'active', expires_at: null }); console.log('Error:', error); } test();
